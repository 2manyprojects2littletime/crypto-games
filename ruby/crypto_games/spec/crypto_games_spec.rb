require 'spec_helper'

describe CryptoGames do
  it 'has a version number' do
    expect(CryptoGames::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
